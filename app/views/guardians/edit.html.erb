<% title "Editing #{@guardian.full_name}" %>

<% semantic_form_for @guardian, :url => family_guardian_url(@guardian.family) do |f| %>
  <%= render 'form', :f => f %>
  <% f.buttons do %>
   <%= f.commit_button 'Update' %>
   <%= f.cancel_link %>
  <% end %>
<% end %>