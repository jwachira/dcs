<% title "Updating #{@provider.name} Profile", :body => true %>

<% semantic_form_for(@provider) do |f| %>
  <%= render 'form', :f => f %>
  <% f.buttons do %>
   <%= f.commit_button 'Update' %>
   <%= f.cancel_link %>
  <% end %>
<% end %>