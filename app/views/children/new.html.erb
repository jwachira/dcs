<% title "New child", :body => true %>

<% semantic_form_for @child, :url => family_children_url(@family), :html => {:multipart => true} do |f| %>
  <%= render 'form', :f => f %>
  <% f.buttons do %>
   <%= f.commit_button 'Submit' %>
  <% end %>
<% end %>

<%= link_to 'Back', children_path %>