<% if @user == current_user %>
  <% title 'Edit My Profile'%>
<% else %>
  <% title 'Edit User' %>
<% end %>
<% error_handling_form_for @user, :url => user_path do |f| %>
  <%= render :partial => "form", :locals => {:f => f} %>
  <%= f.cancel_and_submit%>
<% end %>
