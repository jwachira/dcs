<% title "Editing #{@immunization.name}", :body => true %>

<% semantic_form_for @immunization, :url => object_url do |f| %>
  <%= render 'form', :f => f %>
  <% f.buttons do %>
   <%= f.commit_button 'Update' %>
   <%= f.cancel_link %>
  <% end %>
<% end %>